Для решения задачи принимаем начальное условие, что массив уже задан.
Для создания нового массива необходимо узнать его размерность
С помощью метода поиска размерности массива находим его размерность.
Тело метода: перебераем каждый элемент массива, записываем его в формат строки, узнаём длину строки,
и если длина строки удовлетворяет условию, увеличиваем размерность массива на единицу.
Далее с помощью метода заполняем массив элементами подходящими условию.
Тело метода: задаём массив найденной размерности, заполняем его нулями,
далее перебераем каждый элемент заданного массива и проверяем на заданное услови,
 если оно выполняется записываем значение элемента в результирующий массив.
